# My Node.js Jenkins CI Project

This is a simple Node.js project used to demonstrate Jenkins CI with GitHub integration.

## Build Steps

- npm install  
- npm run build  
- Jenkins archives the `build/` folder as the final artifact.
